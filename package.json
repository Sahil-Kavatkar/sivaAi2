{
  "name": "sih-test-6",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-frontend": "cd frontend && npm install && npm install vite && npx vite build",
    "build-backend": "cd backend && npm install",
    "build": "npm run build-frontend && npm run build-backend",
    "start": "cd backend && npm start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}
